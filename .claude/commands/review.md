---
description: Review the current diff for correctness and simplicity issues.
---

Run `git diff` (and `git diff --staged`) against the working tree. Review changes for:
correctness bugs, leftover debug code, missed edge cases, and unnecessary complexity
(speculative abstractions, dead code, duplicated logic). Report findings as a short
list — no exhaustive narration. Do not modify files unless asked.
