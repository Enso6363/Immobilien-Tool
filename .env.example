# Copy to .env and fill in. .env is gitignored and denied to the agent.
# Use env-var references in .mcp.json, never paste real keys into committed files.

# Example (uncomment what you use):
# ALPHA_VANTAGE_API_KEY=
# OPENAI_API_KEY=
# XAI_API_KEY=
